name: 2. The Executor - Construct Pure APK
on:
  repository_dispatch:
    types: [build_empty_apk]

permissions:
  contents: write

jobs:
  assemble_pure_body:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout Repo
        uses: actions/checkout@v4

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 🏗️ Create Android Project Structure
        run: |
          # إنشاء المجلدات
          mkdir -p app/src/main/java/com/decapitator/ai
          mkdir -p app/src/main/res/layout
          mkdir -p app/src/main/res/values

          # 1. ملف إعدادات المشروع
          cat <<EOF > settings.gradle
          rootProject.name = "DecapitatorAI"
          include ':app'
          EOF

          # 2. ملف Gradle الرئيسي
          cat <<EOF > build.gradle
          buildscript {
              repositories { google(); mavenCentral() }
              dependencies { classpath 'com.android.tools.build:gradle:8.1.1' }
          }
          allprojects {
              repositories { google(); mavenCentral() }
          }
          EOF

          # 3. ملف Gradle الخاص بالتطبيق
          cat <<EOF > app/build.gradle
          plugins { id 'com.android.application' }
          android {
              namespace 'com.decapitator.ai'
              compileSdk 33
              defaultConfig {
                  applicationId "com.decapitator.ai"
                  minSdk 24
                  targetSdk 33
                  versionCode 1
                  versionName "1.0"
              }
          }
          EOF

          # 4. ملف الـ Manifest
          cat <<EOF > app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <application android:label="Decapitator AI" android:theme="@android:style/Theme.Black.NoTitleBar">
                  <activity android:name="com.decapitator.ai.MainActivity" android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF

          # 5. ملف كود الجافا للواجهة (الفارغ بانتظار العقل)
          cat <<EOF > app/src/main/java/com/decapitator/ai/MainActivity.java
          package com.decapitator.ai;
          import android.os.Bundle;
          import androidx.appcompat.app.AppCompatActivity;
          import android.widget.TextView;
          import android.graphics.Color;
          import android.view.Gravity;

          public class MainActivity extends AppCompatActivity {
              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  TextView tv = new TextView(this);
                  tv.setText("THE DECAPITATOR AI\nStatus: Empty Body\nWaiting for Brain Injection...");
                  tv.setTextColor(Color.RED);
                  tv.setTextSize(24);
                  tv.setGravity(Gravity.CENTER);
                  tv.setBackgroundColor(Color.BLACK);
                  setContentView(tv);
              }
          }
          EOF

      - name: 🔨 Build Debug APK
        run: |
          # تحديد المسار لـ Gradle وتنفيذ مهمة البناء للمشروع الفرعي
          chmod +x build.gradle
          gradle :app:assembleDebug

      - name: 📦 Archive APK
        run: |
          mkdir -p final_output
          cp app/build/outputs/apk/debug/app-debug.apk final_output/Decapitator_Pure_Body.apk

      - name: 🚀 Release to GitHub
        uses: softprops/action-gh-release@v1
        with:
          files: final_output/Decapitator_Pure_Body.apk
          name: "The Decapitator - Pure Body v1.0"
          tag_name: "v1.0-pure"
          body: "تم بناء الجسد (APK) بنجاح. الواجهة فارغة تماماً وجاهزة لاستقبال العقل لاحقاً."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}