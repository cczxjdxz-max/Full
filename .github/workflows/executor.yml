name: 2. The Executor - Final Stable APK
on:
  repository_dispatch:
    types: [build_empty_apk]

permissions:
  contents: write

jobs:
  assemble_pure_body:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout Repo
        uses: actions/checkout@v4

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 🏗️ Create Android Modern Structure
        run: |
          mkdir -p app/src/main/java/com/decapitator/ai
          mkdir -p app/src/main/res/layout

          # 1. إعدادات المشروع مع تعطيل تحذيرات التبعيات
          cat <<EOF > settings.gradle
          pluginManagement {
              repositories { google(); mavenCentral(); gradlePluginPortal() }
          }
          dependencyResolutionManagement {
              repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
              repositories { google(); mavenCentral() }
          }
          rootProject.name = "DecapitatorAI"
          include ':app'
          EOF

          # 2. ملف Gradle الرئيسي - تعريف الـ Plugins فقط
          cat <<EOF > build.gradle
          plugins {
              id "com.android.application" version "8.1.1" apply false
          }
          EOF

          # 3. ملف Gradle الخاص بالتطبيق - إصلاح نظام التبعيات
          cat <<EOF > app/build.gradle
          plugins {
              id "com.android.application"
          }
          android {
              namespace "com.decapitator.ai"
              compileSdk 33
              defaultConfig {
                  applicationId "com.decapitator.ai"
                  minSdk 24
                  targetSdk 33
                  versionCode 1
                  versionName "1.0"
              }
          }
          dependencies {
              // استخدام الصيغة الصارمة لمنع أخطاء Gradle 9
              implementation "androidx.appcompat:appcompat:1.6.1"
              implementation "com.google.android.material:material:1.9.0"
              implementation "androidx.constraintlayout:constraintlayout:2.1.4"
          }
          EOF

          # 4. ملف الـ Manifest والكود (كما هما)
          cat <<EOF > app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <application android:label="Decapitator AI" android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">
                  <activity android:name=".MainActivity" android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF

          cat <<EOF > app/src/main/java/com/decapitator/ai/MainActivity.java
          package com.decapitator.ai;
          import android.os.Bundle;
          import androidx.appcompat.app.AppCompatActivity;
          import android.widget.TextView;
          import android.graphics.Color;
          import android.view.Gravity;

          public class MainActivity extends AppCompatActivity {
              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  TextView tv = new TextView(this);
                  tv.setText("THE DECAPITATOR AI\nStatus: Empty Body\nReady for Brain Injection");
                  tv.setTextColor(Color.RED);
                  tv.setTextSize(26);
                  tv.setGravity(Gravity.CENTER);
                  tv.setBackgroundColor(Color.BLACK);
                  setContentView(tv);
              }
          }
          EOF

      - name: 🛠️ Force Gradle Wrapper 8.2 (Stability Fix)
        run: gradle wrapper --gradle-version 8.2

      - name: 🔨 Build Debug APK
        run: ./gradlew :app:assembleDebug --stacktrace

      - name: 📦 Archive & Release
        uses: softprops/action-gh-release@v1
        with:
          files: app/build/outputs/apk/debug/app-debug.apk
          name: "Decapitator Body v1.2"
          tag_name: "v1.2-fixed"
          body: "تم تجاوز قيود Gradle 9. الجسد جاهز للتحميل."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}