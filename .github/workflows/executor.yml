name: 2. The Executor - Kotlin DSL Fixed APK
on:
  repository_dispatch:
    types: [build_empty_apk]

permissions:
  contents: write

jobs:
  assemble_pure_body:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout Repo
        uses: actions/checkout@v4

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 🏗️ Create Android Structure (Kotlin DSL)
        run: |
          mkdir -p app/src/main/java/com/decapitator/ai
          mkdir -p app/src/main/res/layout

          # 1. ملف الإعدادات (Modern Kotlin DSL)
          cat <<EOF > settings.gradle.kts
          pluginManagement {
              repositories { google(); mavenCentral(); gradlePluginPortal() }
          }
          dependencyResolutionManagement {
              repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
              repositories { google(); mavenCentral() }
          }
          rootProject.name = "DecapitatorAI"
          include(":app")
          EOF

          # 2. ملف Gradle الرئيسي
          cat <<EOF > build.gradle.kts
          plugins {
              id("com.android.application") version "8.1.1" apply false
          }
          EOF

          # 3. ملف Gradle الخاص بالتطبيق (إصلاح شامل للتبعيات)
          cat <<EOF > app/build.gradle.kts
          plugins {
              id("com.android.application")
          }
          android {
              namespace = "com.decapitator.ai"
              compileSdk = 33
              defaultConfig {
                  applicationId = "com.decapitator.ai"
                  minSdk = 24
                  targetSdk = 33
                  versionCode = 1
                  versionName = "1.0"
              }
          }
          dependencies {
              implementation("androidx.appcompat:appcompat:1.6.1")
              implementation("com.google.android.material:material:1.9.0")
          }
          EOF

          # 4. ملف الـ Manifest والـ Java (بقيا كما هما ولكن تم وضعهما في مكانهما الصحيح)
          cat <<EOF > app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <application android:label="Decapitator AI" android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">
                  <activity android:name=".MainActivity" android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF

          cat <<EOF > app/src/main/java/com/decapitator/ai/MainActivity.java
          package com.decapitator.ai;
          import android.os.Bundle;
          import androidx.appcompat.app.AppCompatActivity;
          import android.widget.TextView;
          import android.graphics.Color;
          import android.view.Gravity;

          public class MainActivity extends AppCompatActivity {
              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  TextView tv = new TextView(this);
                  tv.setText("THE DECAPITATOR AI\nStatus: Empty Body\nReady for Brain Injection");
                  tv.setTextColor(Color.RED);
                  tv.setTextSize(26);
                  tv.setGravity(Gravity.CENTER);
                  tv.setBackgroundColor(Color.BLACK);
                  setContentView(tv);
              }
          }
          EOF

      - name: 🔨 Build Debug APK
        run: |
          # بناء التطبيق باستخدام الأوامر المباشرة لـ Gradle 9 مع تخطي الـ Daemon لتجنب الانهيار
          gradle :app:assembleDebug --no-daemon

      - name: 📦 Archive & Release
        uses: softprops/action-gh-release@v1
        with:
          files: app/build/outputs/apk/debug/app-debug.apk
          name: "Decapitator Body v1.3"
          tag_name: "v1.3-kotlin"
          body: "تم ترويض Gradle 9 بنجاح باستخدام Kotlin DSL. الجسد جاهز."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}