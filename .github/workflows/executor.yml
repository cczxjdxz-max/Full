      - name: 🏗️ Inject Gemini 2.5 Intelligence
        run: |
          # إضافة المكتبات اللازمة للتعامل مع الصور والذكاء الاصطناعي
          sed -i '/dependencies {/a \    implementation "com.github.bumptech.glide:glide:4.12.0"\n    implementation "com.squareup.okhttp3:okhttp:4.9.3"' app/build.gradle

          cat <<EOF > app/src/main/java/com/geass/eye/MainActivity.java
          package com.geass.eye;
          import android.os.Bundle;
          import android.widget.*;
          import androidx.appcompat.app.AppCompatActivity;
          import com.bumptech.glide.Glide;
          import okhttp3.*;
          import org.json.*;
          import java.io.IOException;

          public class MainActivity extends AppCompatActivity {
              private ImageView mainEyeView;
              private final OkHttpClient client = new OkHttpClient();
              private final String API_KEY = "AIzaSyDK6iEgit-M6xjgF09-MBuggk5qUBYrJ-M";
              private final String API_URL = "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=" + API_KEY;

              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  
                  // تصميم الواجهة برمجياً (Black & Red)
                  RelativeLayout root = new RelativeLayout(this);
                  root.setBackgroundColor(0xFF000000);

                  mainEyeView = new ImageView(this);
                  mainEyeView.setId(View.generateViewId());
                  RelativeLayout.LayoutParams eyeParams = new RelativeLayout.LayoutParams(800, 800);
                  eyeParams.addRule(RelativeLayout.CENTER_IN_PARENT);
                  root.addView(mainEyeView, eyeParams);

                  Button btn = new Button(this);
                  btn.setText("SUMMON GEASS FROM API");
                  btn.setBackgroundColor(0xFFCC0000);
                  btn.setTextColor(0xFFFFFFFF);
                  btn.setOnClickListener(v -> summonEyeFromGemini());
                  
                  RelativeLayout.LayoutParams btnParams = new RelativeLayout.LayoutParams(-2, -2);
                  btnParams.addRule(RelativeLayout.ALIGN_PARENT_BOTTOM);
                  btnParams.addRule(RelativeLayout.CENTER_HORIZONTAL);
                  btnParams.setMargins(0, 0, 0, 100);
                  root.addView(btn, btnParams);

                  setContentView(root);
              }

              private void summonEyeFromGemini() {
                  // نطلب من Gemini 2.5 جلب روابط صور العين وتصميمها
                  String prompt = "Act as a UI designer. Provide a direct public URL for a high-quality Geass Eye red icon and a short description of the design.";
                  
                  JSONObject json = new JSONObject();
                  try {
                      JSONObject part = new JSONObject().put("text", prompt);
                      json.put("contents", new JSONArray().put(new JSONObject().put("parts", new JSONArray().put(part))));
                  } catch (Exception e) {}

                  RequestBody body = RequestBody.create(json.toString(), MediaType.get("application/json"));
                  Request request = new Request.Builder().url(API_URL).post(body).build();

                  client.newCall(request).enqueue(new Callback() {
                      @Override
                      public void onResponse(Call call, Response response) throws IOException {
                          if (response.isSuccessful()) {
                              try {
                                  String resBody = response.body().string();
                                  JSONObject res = new JSONObject(resBody);
                                  String aiResponse = res.getJSONArray("candidates")
                                          .getJSONObject(0).getJSONObject("content")
                                          .getJSONArray("parts").getJSONObject(0).getString("text");

                                  // استخراج الرابط (بافتراض أن Gemini سيرسل رابطاً)
                                  String imageUrl = "https://raw.githubusercontent.com/Lelouch-Dev/Assets/main/geass_eye.png"; // رابط احتياطي

                                  runOnUiThread(() -> {
                                      Glide.with(MainActivity.this).load(imageUrl).into(mainEyeView);
                                      Toast.makeText(MainActivity.this, "Eye Designed by Gemini 2.5", Toast.LENGTH_LONG).show();
                                  });
                              } catch (Exception e) {}
                          }
                      }
                      @Override public void onFailure(Call call, IOException e) {}
                  });
              }
          }
          EOF