name: 2. The Executor - Final Fixed APK
on:
  repository_dispatch:
    types: [build_empty_apk]

permissions:
  contents: write

jobs:
  assemble_pure_body:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout Repo
        uses: actions/checkout@v4

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 🏗️ Create Android Modern Structure
        run: |
          mkdir -p app/src/main/java/com/decapitator/ai
          mkdir -p app/src/main/res/layout

          # 1. ملف إعدادات المشروع
          cat <<EOF > settings.gradle
          pluginManagement {
              repositories { google(); mavenCentral(); gradlePluginPortal() }
          }
          dependencyResolutionManagement {
              repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
              repositories { google(); mavenCentral() }
          }
          rootProject.name = "DecapitatorAI"
          include ':app'
          EOF

          # 2. ملف Gradle الرئيسي (الجذر) - إصدارات حديثة
          cat <<EOF > build.gradle
          plugins {
              id 'com.android.application' version '8.1.1' apply false
              id 'com.android.library' version '8.1.1' apply false
          }
          EOF

          # 3. ملف Gradle الخاص بالتطبيق - توافقية عالية
          cat <<EOF > app/build.gradle
          plugins { id 'com.android.application' }
          android {
              namespace 'com.decapitator.ai'
              compileSdk 33
              defaultConfig {
                  applicationId "com.decapitator.ai"
                  minSdk 24
                  targetSdk 33
                  versionCode 1
                  versionName "1.0"
              }
              buildTypes {
                  release {
                      minifyEnabled false
                  }
              }
          }
          dependencies {
              implementation 'androidx.appcompat:appcompat:1.6.1'
              implementation 'com.google.android.material:material:1.9.0'
          }
          EOF

          # 4. ملف الـ Manifest
          cat <<EOF > app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <application android:label="Decapitator AI" android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">
                  <activity android:name=".MainActivity" android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF

          # 5. كود الجافا للواجهة
          cat <<EOF > app/src/main/java/com/decapitator/ai/MainActivity.java
          package com.decapitator.ai;
          import android.os.Bundle;
          import androidx.appcompat.app.AppCompatActivity;
          import android.widget.TextView;
          import android.graphics.Color;
          import android.view.Gravity;

          public class MainActivity extends AppCompatActivity {
              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  TextView tv = new TextView(this);
                  tv.setText("THE DECAPITATOR AI\nStatus: Empty Body\nWaiting for Brain...");
                  tv.setTextColor(Color.RED);
                  tv.setTextSize(26);
                  tv.setGravity(Gravity.CENTER);
                  tv.setBackgroundColor(Color.BLACK);
                  setContentView(tv);
              }
          }
          EOF

      - name: 🔨 Build Debug APK
        run: |
          # استخدام الـ Wrapper لضمان استقرار البناء
          gradle :app:assembleDebug --warning-mode all

      - name: 📦 Archive APK
        run: |
          mkdir -p final_output
          cp app/build/outputs/apk/debug/app-debug.apk final_output/Decapitator_Pure_Body.apk

      - name: 🚀 Release to GitHub
        uses: softprops/action-gh-release@v1
        with:
          files: final_output/Decapitator_Pure_Body.apk
          name: "Decapitator Body v1.1"
          tag_name: "v1.1-final"
          body: "تم الإصلاح! الجسد الآن متوافق مع Gradle 9 ومبني بأحدث معايير أندرويد."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}