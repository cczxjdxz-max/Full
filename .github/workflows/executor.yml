name: 7. The Ultimate AI Build - Gemini 2.5 Architect
on:
  workflow_dispatch: # يسمح لك بتشغيله يدوياً بضغطة زر

permissions:
  contents: write

jobs:
  build_with_ai_assets:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout Repository
        uses: actions/checkout@v4

      - name: ☕ Setup Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'

      - name: 🧠 Consultant Gemini 2.5 (Design Phase)
        run: |
          # طلب كود الرسم المتجهي (Vector Drawable) من الذكاء الاصطناعي
          RESPONSE=$(curl -X POST "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=AIzaSyDK6iEgit-M6xjgF09-MBuggk5qUBYrJ-M" \
            -H "Content-Type: application/json" \
            -d '{
              "contents": [{
                "parts": [{
                  "text": "Give me ONLY the XML code for an Android Vector Drawable representing a glowing red Geass eye icon. No text explanation, starting with <vector and ending with </vector>."
                }]
              }]
            }')
          
          # استخراج كود الـ XML وتخزينه كملف صورة محقونة
          echo "$RESPONSE" | python3 -c "import sys, json; print(json.load(sys.stdin)['candidates'][0]['content']['parts'][0]['text'].replace('\\\`\\\`\\\`xml', '').replace('\\\`\\\`\\\`', '').strip())" > ic_geass_eye.xml

      - name: 🏗️ Assemble App Structure
        run: |
          rm -rf *
          mkdir -p app/src/main/java/com/geass/eye
          mkdir -p app/src/main/res/drawable
          mkdir -p app/src/main/res/layout
          mkdir -p app/src/main/res/values

          # حقن التصميم المولد من الـ API
          mv ../ic_geass_eye.xml app/src/main/res/drawable/ic_geass_eye.xml || echo "<vector xmlns:android='http://schemas.android.com/apk/res/android' android:width='24dp' android:height='24dp' android:viewportWidth='24' android:viewportHeight='24'><path android:fillColor='#FF0000' android:pathData='M12,2C6.48,2 2,6.48 2,12s4.48,10 10,10 10,-4.48 10,-10S17.52,2 12,2z'/></vector>" > app/src/main/res/drawable/ic_geass_eye.xml

          # 1. gradle.properties
          echo "android.useAndroidX=true" > gradle.properties

          # 2. settings.gradle
          echo "include ':app'" > settings.gradle

          # 3. Root build.gradle
          cat <<EOF > build.gradle
          buildscript {
              repositories { google(); mavenCentral() }
              dependencies { classpath 'com.android.tools.build:gradle:7.4.2' }
          }
          allprojects {
              repositories { google(); mavenCentral() }
          }
          EOF

          # 4. App build.gradle
          cat <<EOF > app/build.gradle
          apply plugin: 'com.android.application'
          android {
              compileSdkVersion 31
              namespace 'com.geass.eye'
              defaultConfig {
                  applicationId "com.geass.eye"
                  minSdkVersion 21
                  targetSdkVersion 31
                  versionCode 7
                  versionName "7.0-AI-Injected"
              }
          }
          dependencies {
              implementation 'androidx.appcompat:appcompat:1.4.1'
              implementation 'com.google.android.material:material:1.5.0'
          }
          EOF

          # 5. AndroidManifest.xml
          cat <<EOF > app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.geass.eye">
              <application android:label="Geass Eye AI" android:theme="@style/Theme.AppCompat.NoActionBar">
                  <activity android:name=".MainActivity" android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF

          # 6. UI Layout (يعرض العين التي صممها Gemini)
          cat <<EOF > app/src/main/res/layout/activity_main.xml
          <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="#000000">
              <ImageView
                  android:layout_width="350dp"
                  android:layout_height="350dp"
                  android:layout_centerInParent="true"
                  android:src="@drawable/ic_geass_eye"
                  android:contentDescription="AI Designed Eye" />
          </RelativeLayout>
          EOF

          # 7. MainActivity.java
          cat <<EOF > app/src/main/java/com/geass/eye/MainActivity.java
          package com.geass.eye;
          import android.os.Bundle;
          import androidx.appcompat.app.AppCompatActivity;
          public class MainActivity extends AppCompatActivity {
              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  setContentView(R.layout.activity_main);
              }
          }
          EOF

      - name: 🔨 Build APK (The AI Shell)
        run: |
          wget -q https://services.gradle.org/distributions/gradle-7.5-bin.zip
          unzip -q gradle-7.5-bin.zip
          ./gradle-7.5/bin/gradle :app:assembleDebug --no-daemon

      - name: 📦 Upload Injected APK
        uses: softprops/action-gh-release@v1
        with:
          files: app/build/outputs/apk/debug/app-debug.apk
          name: "Geass Eye - AI Build v7.0"
          tag_name: "v7.0-ai"
          body: "تم بناء هذا التطبيق وحقنه بتصميم مولد بواسطة Gemini 2.5 Flash."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}