      - name: 1. REAL FORGE & INJECT
        run: |
          echo "📥 Downloading Official Base..."
          wget -q -O base.apk "https://dl.cdn.freefiremobile.com/live/package/FreeFire.apk"
          
          echo "🏗️ Decoding & Injecting..."
          # استخدام apktool الحقيقي لتفكيك الملف
          java -jar apktool.jar d base.apk -o decoded_dir
          
          # إنشاء المجلدات وحقن الملفات (تأكد من وجودها في الخطوات السابقة)
          mkdir -p decoded_dir/assets/python
          echo "print('Sovereign Engine Active')" > decoded_dir/assets/python/internal_master.py
          
          # إعادة البناء (هذا سينتج ملف APK حقيقي)
          java -jar apktool.jar b decoded_dir -o Geass_Internal_Citadel_Unsigned.apk
          
          # التوقيع (ضروري ليكون الملف صالحاً)
          keytool -genkey -v -keystore test.jks -alias test -keyalg RSA -keysize 2048 -validity 10000 -storepass 123456 -keypass 123456 -dname "CN=TEST"
          apksigner sign --ks test.jks --ks-pass pass:123456 --out Geass_Internal_Citadel.apk Geass_Internal_Citadel_Unsigned.apk

      - name: 2. FAST-CHECK (Now with Real Data)
        run: |
          echo "🔍 Analyzing Real Forged APK..."
          # الآن unzip سيعمل لأن الملف أصبح APK حقيقياً وليس مجرد ملف فارغ
          unzip -l Geass_Internal_Citadel.apk | grep "assets/python/internal_master.py" && echo "✅ Python Core: FOUND"
          
          # فحص الـ Manifest بعد التعديل
          # ملاحظة: الـ Manifest داخل الـ APK يكون بصيغة Binary، سنستخدم التقرير النصي من apktool
          grep "SYSTEM_ALERT_WINDOW" decoded_dir/AndroidManifest.xml && echo "✅ Permissions: VERIFIED"