name: GIAS-AI-GRAND-ARCHIVE

on:
  workflow_dispatch:

jobs:
  assemble_archive:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Environment Setup
        run: pip install UnityPy numpy

      - name: 2. APK Deep Extraction
        run: |
          wget -q -O game.apk "https://dl.cdn.freefiremobile.com/live/package/FreeFire.apk"
          unzip -o game.apk -d game_files

      - name: 3. The Grand Assembler (المجمع العملاق)
        run: |
          cat << 'EOF' > grand_assembler.py
          import UnityPy
          import os
          import json

          # قواميس التجميع
          archive = {
              "Bermuda_World": [],    # الخرائط والتضاريس
              "Skins_Library": [],    # الاسكنات والملابس
              "Events_Logic": [],     # الأحداث والمهام
              "Weapons_Master": [],   # الأسلحة والذخيرة
              "Explosives_Data": [],  # القنابل والمتفجرات
              "Characters_Master": [],# الشخصيات والقدرات
              "Physics_Core": [],     # الفيزياء والجاذبية
              "Game_Logic": []        # المنطق العام والسيستم
          }

          # كلمات البحث المفتاحية لكل قسم
          tags = {
              "Bermuda_World": [b"Bermuda", b"Purgatory", b"Kalahari", b"Terrain", b"Map"],
              "Skins_Library": [b"Skin", b"Costume", b"Cloth", b"Bundle"],
              "Events_Logic": [b"Event", b"Mission", b"Quest", b"Reward"],
              "Weapons_Master": [b"Weapon", b"Gun", b"Rifle", b"Ammo", b"Recoil"],
              "Explosives_Data": [b"Grenade", b"Bomb", b"Explosion", b"Flashbang"],
              "Characters_Master": [b"Character", b"Hero", b"Skill", b"Avatar", b"Hitbox"],
              "Physics_Core": [b"Physics", b"Gravity", b"Mass", b"Friction", b"Velocity"],
              "Game_Logic": [b"Manager", b"System", b"Config", b"Setting", b"Rule"]
          }

          print("📦 Starting Grand Assembly of All Assets...")

          for root, dirs, files in os.walk("game_files"):
              for file in files:
                  f_path = os.path.join(root, file)
                  try:
                      env = UnityPy.load(f_path)
                      for obj in env.objects:
                          data_to_save = None
                          
                          # استخراج البيانات بناءً على النوع
                          if obj.type.name in ["MonoBehaviour", "TextAsset"]:
                              try:
                                  data_to_save = obj.read().read_typetree()
                              except:
                                  data_to_save = {"raw_hex": obj.get_raw_data().hex()[:500]}
                          
                          elif obj.type.name == "Mesh":
                              mesh = obj.read()
                              data_to_save = {"mesh_name": mesh.name, "vertices": mesh.m_VertexCount}

                          if data_to_save:
                              # تحديد القسم المناسب للملف
                              raw_content = str(data_to_save).encode()
                              for category, keywords in tags.items():
                                  if any(k in raw_content for k in keywords):
                                      archive[category].append(data_to_save)
                                      break
                  except: continue

          # حفظ كل قسم في ملف واحد ضخم
          print("💾 Saving Master Files...")
          os.makedirs("GRAND_ARCHIVE", exist_ok=True)
          for category, data in archive.items():
              with open(f"GRAND_ARCHIVE/{category}.json", "w", encoding='utf-8') as f:
                  json.dump(data, f, ensure_ascii=False, indent=2)
          
          print("✅ All systems extracted and merged into GRAND_ARCHIVE/")
          EOF
          python3 grand_assembler.py

      - name: 4. Upload Grand Archive
        uses: actions/upload-artifact@v4
        with:
          name: FF_AI_MASTER_DATASET
          path: GRAND_ARCHIVE/